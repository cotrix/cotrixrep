<!DOCTYPE ui:UiBinder SYSTEM "http://dl.google.com/gwt/DTD/xhtml.ent">
<ui:UiBinder xmlns:ui="urn:ui:com.google.gwt.uibinder"
	xmlns:g="urn:import:com.google.gwt.user.client.ui" xmlns:p="urn:import:org.cotrix.web.importwizard.client.progresstracker">
	<ui:with type="org.cotrix.web.importwizard.client.resources.Resources"
		field="resources"></ui:with>
	<ui:style>
		.wizardPanel {
			padding-bottom: 20px;
		}
		
		.progressTracker {
			margin: 0 auto;
			text-align: center;
			margin-top: 37px;
		}
		
		.titlePanel {
			margin: 0 auto;
			padding-top: 34px;
		}
		
		.title {
			font-size: 23px;
			color: #595959;
			text-align: center;
		}
		
		.subtitle {
			font-family: 'Kite One', cursive;
			color: #989898;
			text-align: center;
			margin-top: 2px;
		}
		
		.stepsPanel {
			width: 880px;
			height: 100%;
			margin: 0 auto;
			text-align: center;
		}
		
		.navigationBar {
			font-weight: bold;
			background-color: #FAFAFA;
			height: 40px;
			border-radius: 0px 0px 5px 5px;
			-moz-border-radius: 0px 0px 5px 5px;
		}
		
		.buttonPanel {
			vertical-align: middle;
			margin: 0 auto;
			width: 880px;
		}
		
		.leftButton {
			padding: 5px;
			float: left;
		}
		
		.rightButton {
			padding: 5px;
			float: right;
		}
	</ui:style>
	<g:DockLayoutPanel unit='PX' styleName="{style.wizardPanel}">
		<g:north size='85'>
			<p:ProgressTracker ui:field="progressTracker"
				styleName="{style.progressTracker}" />
		</g:north>
		<g:north size='84'>
			<g:FlowPanel styleName="{style.titlePanel}">
				<g:Label text="Title" styleName="{style.title}" ui:field="title" />
				<g:Label ui:field="subtitle" styleName="{style.subtitle}">If you don't have it,
					we can find it.</g:Label>
			</g:FlowPanel>
		</g:north>
		<g:center>
			<g:DeckLayoutPanel ui:field="stepsPanel" styleName="{style.stepsPanel}" />
		</g:center>
		<g:south size="60">
			<g:HTMLPanel styleName="{style.buttonPanel}">
				<div class="{style.leftButton}">
					<g:Button ui:field="backButton" styleName="{resources.css.grayButton}"
						text="Back" />
				</div>
				<div class="{style.rightButton}">
					<g:Button ui:field="nextButton" styleName="{resources.css.blueButton}"
						text="Next" />
				</div>
				<div class="{style.rightButton}">
					<g:Button ui:field="importButton" styleName="{resources.css.blueButton}"
						text="Import" />
				</div>
				<div class="{style.rightButton}">
					<g:Button ui:field="manageButton" styleName="{resources.css.buttonManage}"
						text="MANAGE IT" />
				</div>
				<div class="{style.rightButton}">
					<g:Button ui:field="newImportButton" styleName="{resources.css.buttonImport}"
						text="IMPORT ANOTHER" />
				</div>
			</g:HTMLPanel>
		</g:south>
	</g:DockLayoutPanel>

</ui:UiBinder> 